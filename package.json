{
  "name": "@lvce-editor/typescript-compile-process-monorepo",
  "version": "0.0.0-dev",
  "description": "TypeScript Compile Process Monorepo",
  "repository": {
    "type": "git",
    "url": "https://github.com/lvce-editor/typescript-compile-process.git"
  },
  "license": "MIT",
  "author": "Lvce Editor",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node packages/build/src/build.js",
    "build:watch": "./node_modules/.bin/esbuild --format=esm --platform=node --bundle --external:electron --external:trash --external:ws --watch packages/typescript-compile-process/src/fileSystemProcessMain.ts --outfile=packages/typescript-compile-process/dist/index.js",
    "dev": "node packages/build/src/dev.js",
    "format": "prettier --write .",
    "lint": "eslint .",
    "test": "npm test --workspaces --if-present",
    "type-check": "tsc -b"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 130
  },
  "devDependencies": {
    "@lvce-editor/eslint-config": "^18.4.0",
    "eslint": "^10.8.1",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
